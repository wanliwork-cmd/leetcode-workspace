# 高级搜索

## 第 1 页

高级搜索

今日目标：

1：

2：

1、搜索回顾

1、朴素搜索

即暴力搜索，时间复杂度高，多数情况下为指数级的。需要优化。

优化方向：重复计算（斐波拉契），剪枝（括号生成问题）

去重：在搜索过程中如果该分支已计算过则直接从缓存（数组，哈希）返回，否则计算并将计算结果缓存

剑指 Offer 10- I. 斐波那契数列

```java
1  class Solution {
```
2

```java
3      public int fib(int n) {
    4          return fib(n,new HashMap());
    5      }
```
6

```java
7      public int fib(int n,Map<Integer,Integer> hash){
```
8 //终止条件

```java
9          if (n < 2 ) {
    10              hash.put(n,n);
    11              return n ;
    12          }else {
        13              if (hash.containsKey(n)) {
            14                  return hash.get(n);
            15              }
            16              int x = fib(n-1,hash) % 1000000007;
            17              hash.put(n-1,x);
            18              int y = fib(n-2,hash) % 1000000007;
            19              hash.put(n-2,y);
            20              int z = (x+y) % 1000000007;
            21              hash.put(n,z);
            22              return  z;
            23          }
            24      }
            25  }
```

## 第 2 页

当然也可以先从子问题开始解决，进而推导大问题的解决（动态规划）

```java
1   class Solution {
    2       public int fib(int n) {
        3           if(n == 0) return 0;
        4           int[] dp = new int[n + 1];
        5           dp[0] = 0;
        6           dp[1] = 1;
        7           for(int i = 2; i <= n; i++){
            8               dp[i] = dp[i-1] + dp[i-2];
            9               dp[i] %= 1000000007;
            10           }
            11           return dp[n];
            12       }
            13   }
```
14 //压缩状态

```java
15   class Solution {
    16       public int fib(int n) {
        17           int a = 0, b = 1, sum;
        18           for(int i = 0; i < n; i++){
            19               sum = (a + b) % 1000000007;
            20               a = b;
            21               b = sum;
            22           }
            23           return a;
            24       }
            25   }

```
剪枝：搜索过程中将不符合需要的分支，不符合最终结果的分支，无法让最终结果达到最优的分支都给拿掉。减少搜索过程，降低时间复杂度。

22. 括号生成

```java
1   class Solution {
```

## 第 3 页

```java
2       public List<String> generateParenthesis(int n) {
    3           List<String> result = new ArrayList<>();
    4           recurGenerate(0,0,n,"",result);
    5           return result;
    6       }
```
7 private void recurGenerate(int left,int right, int n, String

```java
s,List<String> result) {
    8           if (left == n && right == n) {
        9               System.out.println(s);
        10               result.add(s);
        11               return ;
        12           }
        13           if (left < n) {
            14               recurGenerate(left+1,right,n,s+"(",result);
            15           }
            16           if (right < left) {
                17               recurGenerate(left,right+1,n,s+")",result);
                18           }
                19       }
                20   }

```
51. N 皇后

```java
1   class Solution {
    2       public List<List<String>> solveNQueens(int n) {
        3           List<List<String>> res = new ArrayList();
```
4 //特殊判断 n=1 或 n>=4才有解

```java
5           if ((n<1) || (n>1 && n<4 )) {
    6               return res;
    7           }
```
8 //存储n皇后的放置结果,数组下标i代表放在哪行,queens[i]代表放在哪列

```java
9           int[] queens = new int[n];
```
10 Arrays.fill(queens,-1);//代表初始状态
11 //创建三个Set集合，在回溯过程中用于判断在列，撇，捺上是否已经有皇后存在了

```java
12           Set<Integer> lie = new HashSet();
13           Set<Integer> pie = new HashSet();
14           Set<Integer> na =  new HashSet();
15           backtrack(n,0,queens,lie,res);
16           return res;
17       }
```
18 public void backtrack(int n,int row,int[] queens,Set<Integer>

```java
lie,List<List<String>> res) {
```
19 //terminal

```java
20           if (row == n) {
```
21 //根据queens生成返回结果

```java
22               List<String> result = genResult(queens,n);
23               res.add(result);
24               return;
25           }
```
26 //相当于是在行，列，撇，捺四个层面去回溯

```java
27           for (int i=0;i<n;i++) {
```
28 //剪枝1：如果该列上有皇后了则跳过

```java
29               if (lie.contains(i)) {
    30                   continue;
    31               }
```

## 第 4 页

32 //剪枝2：判断撇上是否已经有皇后了:在同一撇上的皇后，行列坐标之和是相等的

```java
33              int rowAddColumn = row + i;
34              if (lie.contains(rowAddColumn)) {
    35                  continue;
    36              }
```
37 //剪枝3：判断捺上是否已经有皇后了：在同一捺上的皇后，行列坐标之差是相等的

```java
38              int rowSubColumn = row - i;
39              if (lie.contains(rowSubColumn)) {
    40                  continue;
    41              }
```
42 //让皇后落在该行该列上

```java
43              queens[row] = i;
```
44 lie.add(i);//该列已经有皇后了
45 lie.add(rowAddColumn);//该撇上已经有皇后了
46 lie.add(rowSubColumn);//该捺上已经有皇后了

```java
47              System.out.println(lie);
```
48 //drill down下探到下一行（下一层）

```java
49              backtrack(n,row+1,queens,lie,res);
```
50 //撤销选择

```java
51              queens[row] = -1;
52              lie.remove(i);
53              lie.remove(rowAddColumn);
54              lie.remove(rowSubColumn);
55          }
56      }
57      public List<String> genResult(int[] queens,int n) {
    58          List<String>  tempResult = new ArrayList();
```
59

```java
60          for (int i=0;i<queens.length;i++) {
    61              char[] row = new char[n];
    62              Arrays.fill(row,'.');
    63              row[queens[i]] = 'Q';
    64              tempResult.add(new String(row));
    65          }
    66          return tempResult;
    67      }
```
68

```java
69  }

```
2、搜索方向：

DFS：深度优先搜索（基于递归，采用栈这种数据结构）

BFS：广度优先搜索（基于遍历，采用了队列这种结构）

优化方向：双向搜索，启发式搜索

双向搜索：在起点和终点处分别做一个广度优先，在中间相遇，时间更快

启发式搜索：采用了一个优先级队列的数据结构，有些节点更符合我们的需要，更能让结果达到最优，则将他们先出队列进行搜索。
2、面试实战

## 第 5 页

34. 解数独

字节，华为，今日头条面试题，37. 解数独

本题的开胃菜：36. 有效的数独

回溯搜索+剪枝

国际站点的几个高效题解：依次解读一下（需要能看懂，能写出来）

https://leetcode.com/problems/sudoku-solver/discuss/15752/Straight-Forward-Java-Sol
ution-Using-Backtracking

https://leetcode.com/problems/sudoku-solver/discuss/15911/Less-than-30-line-clean-ja
va-solution-using-DFS

国内站点一个比较好的题解：

https://leetcode-cn.com/problems/sudoku-solver/solution/pythonsethui-su-chao-guo-9
5-by-mai-mai-mai-mai-zi/

```java
1   class Solution {
```
2

```java
3       Set<Character>[] rowSet ;
4       Set<Character>[] colSet ;
5       Set<Character>[] blockSet ;
```
6

```java
7       public void solveSudoku(char[][] board) {
```
8 //定义行，列，方块的set

```java
9               rowSet = new HashSet[9];
10               colSet = new HashSet[9];
11               blockSet = new HashSet[9];
12               for (int i=0;i<9;i++) {
    13                   rowSet[i] = new HashSet<>();
    14                   colSet[i] = new HashSet<>();
    15                   blockSet[i] = new HashSet<>();
    16               }
```
17
18 //存储剩余还未填充的位置

```java
19               List<int[]> need = new ArrayList();
```
20 //预扫描一遍board

```java
21               for (int i=0;i<9;i++) {
    22                   for (int j=0;j<9;j++) {
        23                       if (board[i][j] != '.') {
            24                           rowSet[i].add(board[i][j]);
            25                           colSet[j].add(board[i][j]);
            26                           int blockIdx = (i / 3) * 3 + j / 3;
            27                           blockSet[blockIdx].add(board[i][j]);
            28                       }else {
                29                           need.add(new int[]{i,j});
                30                       }
                31                   }
                32               }
```
33

```java
34           int k = need.size();
```

## 第 6 页

```java
35           backtrack(board,0,k,need);
```
36

```java
37       }
```
38 //开始回溯搜索
39 public boolean backtrack (char[][] board,int depth,int k,List<int[]>

```java
need) {
    40           if (depth == k) {
        41               return true;
        42           }
        43           int[] cur = need.get(depth);
        44           int row = cur[0];
        45           int col = cur[1];
        46           int block = (row / 3) * 3 + col /3;
        47           for (char i='1';i<='9';i++) {
```
48 //剪枝

```java
49               if (rowSet[row].contains(i)) {
    50                   continue;
    51               }
    52               if (colSet[col].contains(i)) {
        53                   continue;
        54               }
        55               if (blockSet[block].contains(i)) {
            56                   continue;
            57               }
```
58 //可以选择

```java
59               board[row][col] = i;
60               rowSet[row].add(i);
61               colSet[col].add(i);
62               blockSet[block].add(i);
```
63
64 //drill down

```java
65               boolean result = backtrack(board,depth+1,k,need);
66               if (result) {
    67                   return true;
    68               }
```
69 //撤销

```java
70               board[row][col] = '.';
71               rowSet[row].remove(i);
72               colSet[col].remove(i);
73               blockSet[block].remove(i);
```
74

```java
75               //return false;
76           }
```
77 //1~9都选完了还没返回true,说明没有合适的

```java
78           return false;
79       }
80   }

```
在这个题的该解法中，在对剩余每个位置进行搜索选择时，如果先从剩余可选择数量最少的位置开始，则会加快整个搜索进程。

## 第 7 页

127. 单词接龙

字节，微软，谷歌最近面试题，127. 单词接龙

算法分析：

参考优秀题解：

https://leetcode-cn.com/problems/word-ladder/solution/yan-du-you-xian-bian-li-shuang
-xiang-yan-du-you-2/

https://leetcode-cn.com/problems/word-ladder/solution/suan-fa-shi-xian-he-you-hua-ja
vashuang-xiang-bfs23/

1、单词的转换是一个图的搜索，从beginWord到endWord之间可以经过字典wordList中的单词，构成一个图，且是无向图。

2、题目转换为在无向图中搜索从beginWord到endWord之间的最端路径。

解法一：在树/图中求最短路径，直接想到BFS

需要注意的是：在图中使用BFS，除了需要借助一个队列外，还需要一个用于判断某顶点是否已经被访问过的数据结构，可以是数组，哈希，set等等

```java
1  class Solution {
```
2 // BFS广度优先遍历
3 public int ladderLength(String beginWord, String endWord, List<String>

```java
wordList) {
```
4 /*
5将wordList从List转换成Set,因为转换过程中的中间单词必须是字典中的单词
6肯定涉及到要判断转换过程中产生的中间单词是否在字典中,用Set比用List高效
7 */

```java
8          Set<String> wordSet = new HashSet(wordList);
```
9 //特殊如果endWord不在wordSet中提前终止

```java
10          if (!wordSet.contains(endWord)) {
    11              return 0;
    12          }
    13          wordSet.remove(beginWord);
```
14
15 //图的广度优先遍历需要使用队列和表示是否访问过的 visited数据结构(可以是数组,哈希，Set等)

```java
16          Queue<String> queue = new LinkedList();
17          Set<String> visited = new HashSet();
```
18
19 //将beginWord加入队列,搜索开始

```java
20          queue.offer(beginWord);
21          visited.add(beginWord);
```
22 //定义最短转换序列的长度,beginWord算一个长度

```java
23          int minCount = 1;
```
24
25 //上BFS标准模板

```java
26          while  (!queue.isEmpty()) {
```
27 //当前层节点个数

```java
28              int size = queue.size();
29              for (int i=0;i<size;i++) {
```

## 第 8 页

30 //当前单词

```java
31                  String currentWord = queue.poll();
```
32 /*
33如果currentWord能够修改1个字符与endWord相同,则返回 step + 1,
34如果修改1个字符后生成的单词虽然不与endWord相同,但该单词是字典中的,则将该单词加入队列,作为图中下一层的节点
35 */

```java
36                  if (canConvert(currentWord,endWord,queue,visited,wordSet)) {
    37                      return minCount +1;
    38                  }
    39              }
    40              minCount ++;
    41          }
    42          return 0;
    43      }
```
44
45 /*
46判断当前单词可以转换成哪些候选单词（未访问的wordSet中的单词），有两种思路：
47思路1：遍历所有wordSet单词，判断当前单词是否可以转换成这个候选单词。
48判断的过程需要拿当前单词逐个对比wordSet中的每个单词,对比时单词的每一位都都进行比较

```java
49                for (int i=0;i<wordSet.size();i++) {
    50                    for (int j=0;j<currentWord.length();j++) {
```
51 //比较currentWord.charAt(j) 和

```java
wordSet.get(i).charAt(j);
52                    }
53                }
```
54复杂度：O(n * len) n是wordSet中候选单词的个数,len是单词的长度
55
56思路2：因为单词是由 a~z 这有限数量的字符组成的，可以遍历当前单词能转换成的所有单词，判断其是否包含在候选单词中(wordSet)。候选单词用HashSet保存，可以大大提高判断包含关系的性能。

```java
57              for (int j=0;j<currentWord.length();j++) {
```
58 //对currentWord当前这一位用a-z替换生成所有可能的结果单词并判断这些可能的结果是否在wordSet中,如果在则表明找到了图中的下一层节点,加入队列

```java
59                  for (char i='a';i<='z';i++) {
    60                  }
    61              }
```
62
63复杂度：O(len * 26 ) 判断中间结果在wordSet中的复杂度是O(1)的因为wordSet
是Set.
64
65 */
66 public boolean canConvert (String currentWord,String

```java
endWord,Queue<String> queue,Set<String> visited,Set<String> wordSet) {
    67          char[] currentWords = currentWord.toCharArray();
```
68 for (int i=0;i<currentWords.length;i++) { // 题目说明了所有单词具有相同的长度
69 //记录当前这一位的字母,因为每次只能改变一位,在改变后面某一位上的字符时前面的要复原

```java
70              char origin = currentWords[i];
```
71

```java
72              for  (char c='a';c<='z';c++) {
```
73 //如果和当前这一位原本的字符一样则跳过

```java
74                  if (c == origin) {
    75                      continue;
    76                  }
    77                  currentWords[i] = c;
```

## 第 9 页

```java
78                  String newWord = new String(currentWords);
```
79 //当前单词能直接转换成endWord

```java
80                  if (newWord.equals(endWord)) {
    81                      return true;
    82                  }
```
83 //当前单词能转换成字典中单词且newWord未被访问过
84 if (wordSet.contains(newWord) && !visited.contains(newWord))

```java
{
    85                      queue.offer(newWord);
    86                      visited.add(newWord);
    87                  }
    88              }
```
89 //复原当前这一位的字母,开始改变下一位

```java
90              currentWords[i] = origin;
91          }
```
92 //currentWord无法直接转成endWord

```java
93          return false;
94      }
95  }

```
解法二：解法升级，采用双向BFS，能加大搜索效率。

注意：此处的双向并非严格的同时从两个方向进行搜索，只是每次都找最小的集合进行搜索。
每层最小的集合可能存在不同的方向。

```java
1  class Solution {
```
2 public int ladderLength(String beginWord, String endWord, List<String>

```java
wordList) {
    3          Set<String> wordSet = new HashSet(wordList);
    4          if (!wordSet.contains(endWord)) {
        5              return 0;
        6          }
```
7 //用于从beginWord开始搜索

```java
8          Queue<String> queue1 = new LinkedList();
9          Set<String> visited1 = new HashSet();
10          queue1.offer(beginWord);
11          visited1.add(beginWord);
```
12
13 //用于从endWord开始搜索

```java
14          Queue<String> queue2 = new LinkedList();
15          Set<String> visited2 = new HashSet();
16          queue2.offer(endWord);
17          visited2.add(endWord);
```
18
19 //定义最短转换序列的长度

```java
20          int minCount = 0;
```
21

```java
22          while  (!queue1.isEmpty() && !queue2.isEmpty()) {
    23              minCount++;
```
24 /*
25比较一下queue1和queue2中元素个数,我们每次都选择从单词数量小的集合开始扩散
26相当于选择该顶点下一层中顶点个数少的进行搜索，这样搜索总次数是最少的
27 */
28 //我们记queue1是数量少的集合

## 第 10 页

29 if (queue1.size() > queue2.size()) { //交换

```java
30                   Queue<String> temp = queue1;
31                   queue1 = queue2;
32                   queue2 = temp;
```
33 //对应的visited也需要交换

```java
34                   Set<String> vtemp = visited1;
35                   visited1 = visited2;
36                   visited2 = vtemp;
37               }
```
38 //开始进行搜索

```java
39               int size = queue1.size();
40               for (int i=0;i<size;i++) {
    41                   String currentWord = queue1.poll();
    42                   char[] currentWords = currentWord.toCharArray();
    43                   for (int j=0;j<currentWords.length;j++) {
        44                       char origin = currentWords[j];
        45                       for (char c='a';c<='z';c++) {
            46                           currentWords[j] = c;
            47                           String newWord = new String(currentWords);
```
48 //访问过了,跳过

```java
49                           if (visited1.contains(newWord)) {
    50                               continue;
    51                           }
```
52 // 两端遍历相遇，结束遍历，返回count

```java
53                           if (visited2.contains(newWord)) {
    54                               return minCount +1;
    55                           }
```
56 // 如果单词在列表中存在，将其添加到队列，并标记为已访问

```java
57                           if (wordSet.contains(newWord)) {
    58                               queue1.offer(newWord);
    59                               visited1.add(newWord);
    60                           }
    61                       }
```
62 //复位

```java
63                       currentWords[j] = origin;
64                   }
65               }
66           }
```
67

```java
68           return 0;
```
69

```java
70       }
71   }

```
进阶题目：433. 最小基因变化

## 第 11 页

1091. 二进制矩阵中的最短路径

亚马逊，七牛云，深信服面试题，1091. 二进制矩阵中的最短路径

高级搜索：启发式搜索（Heuristic Search / A*）

在图的搜索过程中添加智能化的思考，不断优化搜索的条件，每次选择下一次的搜索路径时都选择最优的，更快能获得最优结果的。

这个过程中需要引入优先级队列(PriorityQueue)，每次在选择搜索下一层邻居的时候不在是使用先进先出的队列，而是按照优先级进行选择，选择一个优先级最高的进行搜索。

参考模板：

```java
1   void bfs (graph,start,end) {
```
2 Queue pq = PriorityQueue();//优先级队列->确定优先级：估价函数

```java
3       pq.add(start);
4       visited.add(start);
```
5

```java
6       while (pq) {
    7           size = pq.size();
    8           for (i:0~size) {
```
9 node = pq.poll();//从队列获取元素时添加智能化/最优化条件判断。

```java
10               visited.add(node);
```
11 //处理当前node

```java
12               process(node);
```
13 //获取node最近一层的邻居，且是未访问过的，加入到队列

```java
14               unvisited = getUnvisitedNeighbor();
15               pq.add(unvisited);
16           }
17       }
18   }

```
在启发式搜索中，最重要的就是如何确定选择的优先级，即所谓的估价函数：

它是一种告知搜索方法的方法，它提供了一种更为明智的方法来猜测哪个邻居节点会导向我们所要查找的终极目标。

```java
1  class Solution {
```
2 //定义8连通方向

```java
3      int[][] direction = new int[][]{
```
4 {-1, -1},
5 {-1, 0},
6 {-1, 1},
7 {0, -1},
8 {0, 1},
9 {1, -1},
10 {1, 0},

```java
11              {1, 1}
```
12

```java
13      };
```
14 //定义网格大小

## 第 12 页

```java
15      int n;
```
16

```java
17      public int shortestPathBinaryMatrix(int[][] grid) {
```
18 // BFS

```java
19          this.n = grid.length;
```
20 // 特殊判断

```java
21          if (grid[0][0] == 1 || grid[n - 1][n - 1] == 1) {
    22              return -1;
    23          }
```
24 //定义优先级队列

```java
25          Queue<Vertex> queue = new PriorityQueue<Vertex>((v1,v2)->{
    26              return v1.f - v2.f;
    27          });
```
28 /*
29 grid有几重作用
30 grid[x][y] ==0 可以走 grid[x][y] ==1 不可以走
31 grid[x][y] == 2,代表走到(x,y)的最短路径为2，直接将走到当前点的路径步数存储到该位置。不会和grid[x][y] ==1冲突的原因是路径最短步数为1
32 */
33 queue.offer(new Vertex(0, 0, grid[0][0] = 1));//起点也算一步
34

```java
35          while (!queue.isEmpty()) {
```
36 Vertex node = queue.poll();//拿出优先级最高的点走
37 int step = grid[node.x][node.y];//获取走到该点的步数
38 //已经走到终点了

```java
39              if (node.x == n - 1 && node.y == n - 1) {
    40                  return step;
    41              }
```
42 //往8个方向上延申

```java
43              for (int[] ints : direction) {
```
44 // 延伸方向

```java
45                  int x = node.x + ints[0];
46                  int y = node.y + ints[1];
```
47 // 边界条件,不能走出网格

```java
48                  if (x < 0 || y < 0 || x >= n || y >= n) {
    49                      continue;
    50                  }
```
51 /*
52能走的路只有grid[x][y]==0、或者之前走过但是步数过多的点(当前这条路径从该点走的步数更少)
53另外如果当前点原本不可走即grid[x][y]==1,由于step+1>1所以肯定也是不会走该点的。
54 */

```java
55                  if (grid[x][y] == 0 || grid[x][y] > step + 1) {
    56                      queue.offer(new Vertex(x, y, grid[x][y] = step + 1));
    57                  }
    58              }
    59          }
    60          return -1;
```
61

```java
62      }
```
63
64
65 //定义顶点

```java
66      class Vertex {
    67          int x, y, f;
    68          public Vertex(int x, int y, int step) {
        69              this.x = x;
```

## 第 13 页

```java
70              this.y = y;
```
71 // 曼哈顿距离变体

```java
72              int diff = Math.max(n - x, n - y);
```
73 this.f = diff + step; // 离右下角近的点优先计算使用曼哈顿距离并且加上当前的步数,因为有可能两个点的diff一样，但是走到这两个点的步数不一样

```java
74          }
75      }
76  }

```
