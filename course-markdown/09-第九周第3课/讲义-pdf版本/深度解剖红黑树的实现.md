# 深度解剖红黑树的实现

## 第 1 页

深度解剖红黑树的实现

今日目标：

1：能说出红黑树删除的几种逻辑及处理方式

2：完成红黑树删除逻辑的代码实现
1、红黑树的实现-续

首先，红黑树也是一棵二叉搜索树，对于二叉搜索树的删除，总共可以分为3中情况：

1、删除度为0的节点：直接删除

2、删除度为1的节点：父节点指向其子节点

3、删除度为2的节点：用前驱节点或后继节点替换删除节点，真正被删除的是用以替换的前驱或者后继节点，即回到了前两种情况。

1.1、删除的所有情况

通过对BST树删除情况的分析，红黑树的删除总共有如下几种情况：

## 第 2 页

1.2、删除度为1的黑色节点

这种情况是：删除的是黑色节点，且它有一个红色替代子节点（只可能是红色不可能是黑色，因为如果是黑色违反了红黑树的性质5）

判断条件：用以替代的子节点是红色

解决方案：将替代的子节点染黑色即可。

1.3、删除度为0的黑色节点

1.3.1、度为0的根节点

如果被删除节点度为0，且是根节点，则直接删除即可，无需做任何调整。

1.3.2、度为0的非根节点

1.3.2.1、兄弟节点是黑色

1.3.2.1.1、黑兄弟有红色子节点

被删除节点在左边：父节点RR/RL旋转，旋转后根节点继承父节点颜色，且根节点左右子节点染黑色

被删除节点在右边:父节点LL/LR旋转，旋转后根节点继承父节点颜色，且根节点左右子节点染黑色

## 第 3 页

1.3.2.1.2、黑兄弟无红色子节点

1.3.2.2、兄弟节点是红色

需要经过操作后回到兄弟节点是黑色的情况

## 第 4 页

1.4、删除总结

1.5、删除实现

1、删除某个节点，则需要首先找到被删除的节点

1 /**
2 * 删除节点
3 * @param key
4 */

```java
5       public void remove(int key) {
    6           remove(node(key));
    7       }
    8       private Node node(int key) {
```
9 //从根节点开始查找要删除的节点

```java
10           Node node = root;
11           while (node!=null) {
```
12 if (key > node.key) { //去右子树找

```java
13                   node = node.right;
```
14 }else if (key < node.key){ //去左子树找

```java
15                   node = node.left;
16               }else {
```
17 //找到了要删除的节点

```java
18                   return node;
19               }
20           }
21           return null;
22       }
```
23 /**
24 * 删除节点
25 * @param node 被删除节点
26 */

```java
27       private void remove(Node node) {
    28           if (node == null) {
        29               return;
        30           }
```
31 //如果是度为2的节点

## 第 5 页

```java
32          if (node.hasTwoChildren()) {
```
33 //用后继节点替换当前节点,真正要删除的是后继节点

```java
34              Node p = node.right;
35              while (p.left !=null) {
    36                  p = p.left;
    37              }
```
38 //用后继节点的值覆盖度为2的节点的值

```java
39              node.key = p.key;
```
40 //真正要被删除的是其后继节点

```java
41              node = p;
42          }
```
43
44 //真正被删除的节点是度为0和度为1的节点
45 //找到替代节点

```java
46          Node replacement = node.left !=null? node.left:node.right;
```
47 if (node.parent == null) { //node是根节点

```java
48              root = replacement;
49          }else if (node.isLeftChild()) {
    50              node.parent.left = replacement;
    51          }else {
        52              node.parent.right =replacement;
        53          }
        54          if (replacement!=null) {
            55              replacement.parent = node.parent;
            56          }
```
57 //删除后的调整---后文实现

```java
58          afterRemove(node,replacement);
59      }

```
2、实现afterRemove完成删除后的调整，按照整理好的逻辑编写即可

1 /**
2 * 删除后的调整
3 * @param beRemoved
4 * @param replacement
5 */

```java
6      private void afterRemove(Node beRemoved,Node replacement) {
```
7 //被删除节点是红色，无需调整

```java
8          if (isRed(beRemoved)) {
    9              return;
    10          }
```
11 //被删除节点是黑色
12 //被删除节点是黑色且有红色替代子节点，将其染黑即可

```java
13          if (isRed(replacement)) {
    14              black(replacement);
    15              return;
    16          }
```
17 //被删除节点是黑色叶子且是根节点无需操作了

```java
18          Node parent = beRemoved.parent;
19          if (parent == null) {
    20              return;
    21          }
```
22
23 //被删除节点是黑色叶子，两种情况：通过兄弟节点是黑色还是红色来判断，且被删除节点在左在右逻辑不一样

## 第 6 页

```java
24           boolean left = parent.left == null || beRemoved.isLeftChild();
25           Node sibling = left ? parent.right:parent.left;
```
26
27 if (left) { //被删除的节点在左边，兄弟节点在右边
28 //兄弟节点是红色

```java
29               if (isRed(sibling)) {
    30                   black(sibling);
    31                   red(parent);
    32                   LLrotate(parent);
    33                   sibling = parent.right;
    34               }
```
35
36 //兄弟是黑色
37 //黑兄弟无红色子节点

```java
38               if (isBlack(sibling.left) && isBlack(sibling.right)) {
    39                   boolean parentBlack = isBlack(parent);
    40                   red(sibling);
    41                   black(parent);
    42                   if (parentBlack) {
        43                       afterRemove(parent,null);
        44                   }
```
45 }else { //黑兄弟至少有一个红色子节点 LL,LR

```java
46                   Node newParent;
47                   if (isRed(sibling.left)) {
    48                       newParent= RRrotate(parent);
    49                       color(newParent,colorOf(parent));
    50                   }else {
        51                       newParent = RLrotate(parent);
        52                       color(newParent,colorOf(parent));
        53                   }
        54                   black(newParent.left);
        55                   black(newParent.right);
        56               }
```
57 }else { // 被删除的节点在右边，兄弟节点在左边
58 //判兄弟节点是红色

```java
59               if (isRed(sibling)) {
    60                   black(sibling);
    61                   red(parent);
    62                   LLrotate(parent);
    63                   sibling = parent.left;
    64               }
```
65
66 //兄弟是黑色
67 //黑兄弟无红色子节点

```java
68               if (isBlack(sibling.left) && isBlack(sibling.right)) {
    69                   boolean parentBlack = isBlack(parent);
    70                   red(sibling);
    71                   black(parent);
    72                   if (parentBlack) {
        73                       afterRemove(parent,null);
        74                   }
```
75 }else { //黑兄弟至少有一个红色子节点 LL,LR

```java
76                   Node newParent;
77                   if (isRed(sibling.left)) {
    78                       newParent= LLrotate(parent);
    79                       color(newParent,colorOf(parent));
    80                   }else {
        81                       newParent = LRrotate(parent);
```

## 第 7 页

```java
82                      color(newParent,colorOf(parent));
83                  }
84                  black(newParent.left);
85                  black(newParent.right);
86              }
87          }
```
88

```java
89      }

```
3、添加测试方法

```java
1  public static void testRemove() {
    2      int[] data = new int[] {55, 87, 56, 74, 96, 22, 62, 20, 70, 68, 90, 50};
    3      RBTree rbTree = new RBTree();
    4      for (int i = 0; i < data.length; i++) {
        5          rbTree.add(data[i]);
        6      }
        7      BinaryTrees.println(rbTree);
        8      for (int i = 0; i < data.length; i++) {
            9          rbTree.remove(data[i]);
            10          System.out.println("---------------------------------------");
            11          System.out.println("【" + data[i] + "】");
            12          BinaryTrees.println(rbTree);
            13      }
            14  }

```
4、可借助https://www.cs.usfca.edu/~galles/visualization/RedBlack.html进行测试

注意：删除的结果不唯一，该网站在删除度为2的节点时，采用的是用其前驱节点替代，同时删除逻辑中也有一处可采用不同的逻辑。
