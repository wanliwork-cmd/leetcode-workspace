# 传说中的红黑树它来了

## 第 1 页

传说中的红黑树它来了

今日目标：

1：能说出红黑树的几个特点

2：能说出红黑树添加节点的几种情况及如何调整

3：完成红黑树添加节点的代码实现

1、红黑树的定义及特点

在前面的章节中我们讲到了AVL树这样一个平衡二叉查找树数据结构，它能够做到插入，删除，查询的时间复杂度为O(log n)，AVL树是一种非常严格的平衡二叉树并且它也是自平衡的，AVL树有很多好处但也有弊端，AVL 树为了维持这种高度的平衡，就要付出更多的代价。每次插入、删除都要做调整，就比较复杂、耗时。所以，对于有频繁的插入、删除操作的数据集合，使用 AVL 树的代价就有点高了。

其实平衡二叉查找树有很多，接下来我们学习一种数据结构叫红黑树，红黑树是一种比较难的数据结构，但是又由于红黑树是一种性能非常稳定的二叉查找树，所以，在工程中，但凡是用到动态插入、删除、查找数据的场景，都可以用到它。红黑树也是为了解决普通二叉查找树在数据更新的过程中，复杂度退化的问题而产生的。

红黑树（Red Black Tree）：也是一种自平衡的二叉搜索树，之前叫做平衡二叉B树（Symmetric
Binary B-Tree）。

首先，红黑树也是一棵二叉搜索树BST，因此它也满足BST树的所有特征。除此之外，它必须满足以下5条性质：

性质1：节点具备颜色，要么是红色（RED）,要么是黑色（BLACK）。

性质2：根节点是黑色（BLACK）。

性质3：叶子节点都是黑色（BLACK）的空节点，也不存数据。

注意：

## 第 2 页

1、红黑树中的叶子节点并非我们之前所讲的叶子节点，红黑树中的叶子节点是假想出来的，是为了配合它的一些性质而产生的（在java中就是为null的空节点）。

2、红黑树这样定义叶子节点，会让原来度为0及度为1的节点都变成度为2的节点。

另外：在后续讲解过程中，我们将叶子节点省略，但要认识到它的存在！

性质4：红黑树中，红色（RED）节点的子节点都是黑色（BLACK）

注意：由红黑树的这一性质可推导出两个结论

1、红色节点的父节点都是黑色

2、从根节点到叶子节点的所有路径上不能有2个连续的红色（RED）节点

性质5：从任一节点到叶子节点的所有路径都包含相同数目的黑色（BLACK）节点

总之：在这些规则的约束下，红黑树能够保证平衡。

知识小测验：请判断以下这棵树是否是红黑树

2、红黑树的实现

红黑树整体实现起来比较复杂，代码实现难度高，因此需要分步来实现。而需要我们重点关注的就是红黑树元素插入和删除操作。

本节中来实现红黑树的插入/添加操作。
2.1、前期准备

1、首先需要明确几个定义：

## 第 3 页

parent：父节点

sibling：兄弟节点

uncle：叔父（parent的兄弟，即叔叔）节点

grand：祖父节点（parent的父节点）

2、对于插入的新节点，我们推荐它是红色

1、如果新添加的节点是黑色，会违反性质5，想要进行修正不太容易

2、如果新添加的节点是红色，可能会违反性质4，想要进行修正有固定的方法，比如染色，旋转

另外：如果添加的节点是要作为根节点，直接染成BLACK即可。

3、创建红黑树： com.itheima.tree.RBTree，同时我们需要

定义红黑树的节点Node
同时需要一些辅助函数，在后续编程过程中使用

```java
1  package com.itheima.tree;
```
2

```java
3  import com.itheima.printer.BinaryTreeInfo;
```
4
5 /**
6 * Created by 传智教育*黑马程序员.
7 */

```java
8  public class RBTree  implements BinaryTreeInfo{
```
9

```java
10      private static final boolean RED = false;
11      private static final boolean BLACK = true;
```
12
13 Node root;//红黑树根节点
14
15
16
17 ///-------------------------辅助函数------------------------
18

## 第 4 页

19 /**
20 * 给节点node染色并返回该节点
21 * @param node
22 * @param color
23 * @return
24 */

```java
25       private Node color(Node node,boolean color) {
    26           if (node == null) {
        27               return node;
        28           }
        29           node.color = color;
        30           return node;
        31       }
```
32
33 /**
34 * 将节点染成红色
35 * @param node
36 * @return
37 */

```java
38       private Node red(Node node) {
    39           return color(node,RED);
    40       }
```
41
42 /**
43 * 将节点染成黑色
44 * @param node
45 * @return
46 */

```java
47       private Node black(Node node) {
    48           return color(node,BLACK);
    49       }
```
50
51 /**
52 * 返回节点的颜色
53 * @param node
54 * @return
55 */

```java
56       public boolean colorOf(Node node) {
    57           return node == null ? BLACK:node.color;
    58       }
```
59
60 /**
61 * 判断节点是否是黑色
62 * @param node
63 * @return
64 */

```java
65       private boolean isBlack(Node node) {
    66           return colorOf(node) == BLACK;
    67       }
```
68
69 /**
70 * 判断节点是否是红色
71 * @param node
72 * @return
73 */

```java
74       private boolean isRed(Node node) {
    75           return colorOf(node) == RED;
    76       }
```

## 第 5 页

77
78 ///------------BinaryTreeInfo接口的几个实现
79 @Override

```java
80       public Object root() {
    81           return root;
    82       }
```
83
84 @Override

```java
85       public Object left(Object node) {
    86           return ((Node)node).left;
    87       }
```
88
89 @Override

```java
90       public Object right(Object node) {
    91           return ((Node)node).right;
    92       }
```
93
94 @Override

```java
95       public Object string(Object node) {
    96           return node;
    97       }
```
98
99 //节点的定义

```java
100       public static class Node  {
    101           int key;
```
102 boolean color = RED;//代表节点的颜色 RED=false BLACK=true

```java
103           Node left;
104           Node right;
105           Node parent;
```
106

```java
107           public Node(int key, Node parent) {
    108               this.key = key;
    109               this.parent = parent;
    110           }
```
111
112 /**
113 * 判断当前节点是否是最后一层的叶子(并非指红黑树中的叶子节点)
114 * @return
115 */

```java
116           public boolean isLeaf() {
    117               return left==null && right == null;
    118           }
```
119
120 /**
121 * 判断是否有两个子节点
122 * @return
123 */

```java
124           public boolean hasTwoChildren() {
    125               return left != null && right != null;
    126           }
```
127
128 /**
129 * 判断当前节点是否是其父节点的左子节点
130 * @return
131 */

```java
132           public boolean isLeftChild() {
    133               return parent != null && this == parent.left;
    134           }
```

## 第 6 页

135
136 /**
137 * 判断当前节点是否是其父节点的右子节点
138 * @return
139 */

```java
140           public boolean isRightChild() {
    141               return parent != null && this == parent.right;
    142           }
```
143
144 /**
145 * 获取当前节点的兄弟节点
146 * @return
147 */

```java
148           public Node sibling() {
    149               if (isLeftChild()) {
        150                   return parent.right;
        151               }
```
152

```java
153               if (isRightChild()) {
    154                   return parent.left;
    155               }
    156               return null;
    157           }
```
158
159
160 @Override

```java
161           public String toString() {
    162               String str = "";
    163               if (color == RED) {
        164                   str = "R_";
        165               }
        166               return str +key;
        167           }
        168       }
        169   }

```
2.2、添加的所有情况

1、首先要明确，在一棵红黑树中添加节点，新的节点一定是在最下面一层，因为红黑树本身也还是一棵BST树

2、如果我们想在下面这课红黑树中添加一个节点（红色节点），所有的情况加起来一共12种。

## 第 7 页

这12种情况分别如下图所示：

而这12种情况其实可以分成两大类：

父节点是黑色（共4中情况）：仍然满足红黑树的定义，无需任何操作

父节点是红色（共8中情况）：违反了红黑树性质4，红色节点的子节点必须是黑色，此时需要做出调整

## 第 8 页

2.3、添加-修复性质4

可分为两种情况:

情况1：叔父节点不是红色

1、符合RR,LL模式，处理方式如下：

祖父节点进行单旋转操作，RR左旋，LL右旋染色：父节点染成黑色，祖父节点染成红色

RR：对祖父节点左旋

LL：对祖父节点右旋

调整后的结果如下图所示：

## 第 9 页

2、符合 LR,RL模式，处理如下：

进行旋转操作，LR:父节点左旋，祖父右旋，RL:父节点右旋，祖父左旋染色：自己染成黑色，祖父节点染成红色

调整之后的结果如下图所示：

## 第 10 页

情况2：叔父节点是红色

不论是LL ,LR ,RR ,RL中的哪种情况，处理方式均一样，只需染色，无需旋转。

调整过程如下：

向上合并：

## 第 11 页

添加：总结

添加的所有情况分析完成后，可总结如下：

2.4、添加的实现

1、定义add方法

```java
1  public void add (int key) {
```
2 //判断传入元素
3 //判断root是否为空

```java
4          if (root == null) {
    5              root = new Node(key,null);
    6              afterAdd(root);
    7              return;
    8          }
```
9
10 //找到新节点应插入到哪个位置

```java
11           Node parent = null;
```

## 第 12 页

```java
12          Node curr = root;
13          while ( curr!= null ) {
    14              parent = curr;
    15              if (key > curr.key) {
        16                  curr = curr.right;
        17              }else if (key < curr.key) {
            18                  curr = curr.left;
            19              }
            20          }
```
21 //判断新节点是插入到parent的左子树还是右子树上

```java
22          Node newNode = new Node(key,parent);
```
23 if (key > parent.key) { //插入到右子树上

```java
24              parent.right = newNode;
```
25 }else if (key < parent.key){ //插入到左子树上

```java
26              parent.left = newNode;
27          }
```
28

```java
29          afterAdd(newNode);
```
30

```java
31      }
```
32
33 /**
34 * 添加完成后的红黑树进行调整
35 * @param node新添加的节点
36 */

```java
37      public void afterAdd(Node node){
    38          Node parent = node.parent;
```
39 //如果新添加的节点是根节点或者向上调整到了根节点

```java
40          if (parent==null) {
    41              black(node);
    42              return;
    43          }
```
44
45 //如果父节点是黑色无需调整

```java
46          if (isBlack(parent)) {
    47              return;
    48          }
```
49
50 //如果父节点是红色则又分两种逻辑（叔父节点不是红色/叔父节点是红色）
51
52 Node uncle = parent.sibling();//叔父节点
53 Node grand = red(parent.parent);//祖父节点-直接染成红色:因为后面所有情况都需要将其染成红色
54
55 /**
56 * 叔父节点是红色：
57 * 染色：父节点和叔父节点染黑色,祖父染红色
58 * 向上合并：将祖父节点当作新添加的节点继续整个这套流程
59 */

```java
60          if (isRed(uncle)) {
    61              black(parent);
    62              black(uncle);
    63              //red(grand);
    64              afterAdd(grand);
    65              return;
    66          }
```
67
68 /**

## 第 13 页

69 * 叔父节点不是红色：判断当前是属于：RR,LL,LR,RL中的哪种情况
70 * RR/LL：
71 1、染色：父节点染成黑色，
72祖父节点染成红色
73 2、祖父节点进行旋转操作
74分别为 RR旋转和LL旋转
75 * LR/RL:
76 1、进行旋转操作
77 LR:父节点左旋，祖父右旋
78 RL:父节点右旋，祖父左旋
79
80 2、染色：自己染成黑色，
81祖父节点染成红色
82 */
83 if (parent.isLeftChild()) { // L
84 if (node.isLeftChild()) { //LL

```java
85                   black(parent);
86                   //red(grand);
87                   LLrotate(grand);
```
88 }else { // LR

```java
89                   black(node);
90                   //red(grand);
91                   LRrotate(grand);
92               }
```
93 }else { //R
94 if (node.isLeftChild()) { //RL

```java
95                   black(node);
96                   //red(grand);
97                   RLrotate(grand);
```
98 }else { // RR

```java
99                   black(parent);
100                  //red(grand);
101                   RRrotate(grand);
102               }
103           }
104       }
```
105

```java
106       private Node RRrotate(Node node) {
    107           Node newParent = node.right;
    108           node.right = newParent.left;
    109           if (newParent.left !=null) {
        110               newParent.left.parent = node;
        111           }
        112           newParent.left = node;
        113           newParent.parent = node.parent;
        114           if (node.isLeftChild()) {
            115               node.parent.left = newParent;
            116           }else if (node.isRightChild()){
                117               node.parent.right = newParent;
```
118 }else { //node是根节点

```java
119               root = newParent;
120           }
121           node.parent = newParent;
122           return newParent;
123       }
```
124

```java
125       private Node LLrotate(Node node) {
    126           Node newParent  = node.left;
```

## 第 14 页

```java
127           node.left = newParent.right;
128           if (newParent.right!=null) {
    129               newParent.right.parent = node;
    130           }
    131           newParent.right = node;
    132           newParent.parent = node.parent;
    133           if (node.isLeftChild()) {
        134               node.parent.left = newParent;
        135           }else if (node.isRightChild()) {
            136               node.parent.right = newParent;
            137           }else {
                138               root = newParent;
                139           }
                140           node.parent = newParent;
                141           return newParent;
                142       }
```
143

```java
144       private Node RLrotate(Node node) {
    145           LLrotate(node.right);
    146           return RRrotate(node);
    147       }
```
148

```java
149       private Node LRrotate(Node node) {
    150           RRrotate(node.left);
    151           return LLrotate(node);
    152       }
```
153

2、创建测试类： com.itheima.tree.RBTreeTest

```java
1   package com.itheima.tree;
```
2
3

```java
4   import com.itheima.printer.BinaryTrees;
```
5
6 /**
7 * Created by 传智教育*黑马程序员.
8 */

```java
9   public class RBTreeTest {
```
10

```java
11       public static void main(String[] args) {
    12           testAdd();
    13       }
```
14

```java
15       public static void testAdd() {
    16           int[] data =  {55, 87, 56, 74, 96, 22, 62, 20, 70, 68, 90, 50};
    17           RBTree rbTree = new RBTree();
    18           for (int i = 0; i < data.length; i++) {
        19               rbTree.add(data[i]);
        20               System.out.println("【" + data[i] + "】");
        21               BinaryTrees.println(rbTree);
        22               System.out.println("---------------------------------------");
        23           }
        24       }
```
25

## 第 15 页

```java
26   }

```
3、利用可视化网站进行测试：https://www.cs.usfca.edu/~galles/visualization/RedBlack.html
