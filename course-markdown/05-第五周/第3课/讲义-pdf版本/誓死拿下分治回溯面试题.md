# 誓死拿下分治回溯面试题

## 第 1 页

誓死拿下分治回溯面试题

47. 全排列 II

字节，华为最近面试题，47. 全排列 II

这一题在力扣第 46 题： 全排列的基础上增加了序列中的元素可重复这一条件，但要求：返回的结果又不能有重复元素。

如何理解：参考精选题解：

所谓返回结果的重复：

重复即为：存在相同数字，比如 [1,2,2']，那么答案 [1,2,2'] 和 [1,2',2] 就其实是一样的，在保存结果的时候，我们只需要一个。

我们能想到的最简单的做法可能会有下面几种：

1：先生成完所有的结果，然后对结果集进行去重

2：每次生成结果时先判断是否已有这个结果了，如果有了则丢弃（全排列终止时判断）

这两种办法均不太好的地方在于：

1：我们生成的结果并非字符串这种简单结构，每个结果是 <List<Integer>，最终返回
List<List<Integer>>

无论是去重，还是判断结果是否已存在都不太好操作

2：及时能够这样做，但是最终的复杂度也非常高。

那么我们能不能在生成答案的过程中就将其剪枝（类比用过的数字就不考虑），这样根本就不会生成重复的答案了。答案是可以的，具体思路我们可以借鉴精选题解：https://leetcode-
cn.com/problems/permutations-ii/solution/hot-100-47quan-pai-lie-ii-python3-hui-su-kao-lu-zh/中对于如何剪枝的图解部分！

核心思想就是：

1：考虑重复元素一定要优先排序，将重复的都放在一起，便于找到重复元素和剪枝！！！

2：剪枝条件是：和前一个元素值相同（index>0），并且前一个元素还没有被使用过

因为每次在处理当前层的时候，如果前一个重复元素没有使用过，那么它一定会出现在当前重复元素的下一层可选择列表中，那最终枚举出来的结果就一定会重复。（和前一个重复元素使用后，当前重复元素出现在它的下一层可选择列表中枚举出来的结果重复）

## 第 2 页

当然：在整个代码编写过程中，需要有两处剪枝，一处是正常全排列要记录哪些已经走过了，第二处就是刚刚说的遇到重复元素的剪枝；当然第二个剪枝依赖于对可选择列表排序！

```java
1    class Solution {
    2        public List<List<Integer>> permuteUnique(int[] nums) {
        3            List<List<Integer>> res = new ArrayList();
        4            boolean[] used = new boolean[nums.length];
        5            Deque<Integer> path = new ArrayDeque();
```
6 //对nums排序，是剪枝2的前提

```java
7            Arrays.sort(nums);
8            backtrack(nums,1,path,used,res);
9            return res;
10        }
```
11
12 public void backtrack(int[] nums,int dept,Deque<Integer> path,boolean[]

```java
used,List<List<Integer>> res) {
```
13 //terminal

```java
14            if (dept > nums.length) {
    15                res.add(new ArrayList(path));
    16                return;
    17            }
```
18 //process current logic

```java
19            for (int i=0;i<nums.length;i++) {
```
20 if (used[i]) { //剪枝1

```java
21                    continue;
22                }
```
23 if (i>0 && nums[i] == nums[i-1] && !used[i-1]) {//剪枝2

```java
24                    continue;
25                }
```
26

```java
27                path.addLast(nums[i]);
28                used[i] = true;
```
29
30 //drill down

```java
31                backtrack(nums,dept+1,path,used,res);
```
32
33 //backtrack

```java
34                path.removeLast();
35                used[i] = false;
36            }
37        }
38    }
```

## 第 3 页

78. 子集

快手，腾讯，华为面试题，78. 子集

回溯算法，套通用模板！

选择的过程及构成的决策树如下，

```java
1   class Solution {
    2       public List<List<Integer>> subsets(int[] nums) {
        3           List<List<Integer>> res = new ArrayList();
        4           Deque<Integer> path = new ArrayDeque();
        5           backtrack(nums,0,path,res);
        6           return res;
        7       }
```
8
9
10 public void backtrack(int[] nums,int start,Deque<Integer> path,List<List<Integer>> res)

```java
{
```
11 //每下探一层都会记录中间结果!

```java
12            res.add(new ArrayList(path));
```
13
14 //这里for循环结束就是终止条件了

```java
15            for (int i=start;i< nums.length;i++) {
```
16 //做出选择

```java
17                    path.addLast(nums[i]);
```
18 //下探到下一层

19 backtrack(nums,i+1,path,res);//注意start传的是i+1，找的就是子集!

## 第 4 页

20 //撤销本层选择,回溯发生的地点

```java
21                   path.removeLast();
22           }
23       }
24   }

```
注意：

1：这道题和全排列终止的条件不一样，全排列是到树的底部后我们才需要记录排列结果，而子集这道题，我们是选择一步就需要记录一下结果，因为每选择的一步都会构成一个新的子集。

2：子集这道题和全排列他们的选择列表处理形式不太一样，全排列每次选择都可以从数组nums中任意选择，只不过我们通过visited/used来剪枝，以前选过的就不选了；而子集这道题我们是通过循环开始
start来逐步缩小子集的范围。

此题还可以用数学归纳法思想，结合递归来求解！

90. 子集 II

字节跳动面试题，90. 子集 II

跟47，全排列II 一样，在搜索子集的过程中去重，判断重复的条件是一样的。

```java
1   class Solution {
    2       public List<List<Integer>> subsetsWithDup(int[] nums) {
        3           List<List<Integer>> res = new ArrayList();
        4           Deque<Integer> path = new ArrayDeque();
        5           boolean[] used = new boolean[nums.length];
        6           Arrays.sort(nums);
        7           backtrack(nums,0,path,used,res);
        8           return res;
        9       }
```
10 public void backtrack(int[] nums,int start,Deque<Integer> path,boolean[]

```java
used,List<List<Integer>> res) {
```
11 //每下探一层都会记录中间结果!

```java
12           res.add(new ArrayList(path));
```
13
14 //这里for循环结束就是终止条件了
15 for (int i=start;i< nums.length;i++) { //子集，通过控制选择列表的起始位置start达到剪枝效果
16 //剪枝2

```java
17                   if (i>0 && nums[i] == nums[i-1] && !used[i-1]) {
    18                       continue;
    19                   }
```
20 //做出选择

```java
21                   path.addLast(nums[i]);
22                   used[i] = true;
```
23 //下探到下一层

## 第 5 页

24 backtrack(nums,i+1,path,used,res);//注意level传的是i+1，找的就是子集!
25 //撤销本层选择,回溯发生的地点

```java
26                    path.removeLast();
27                    used[i] = false;
28            }
29        }
30    }

```
77. 组合

字节，阿里面试题，77. 组合

跟子集的思考方式一模一样！故代码编写起来也是差不多，还是回溯思想！

```java
1   class Solution {
    2       public List<List<Integer>> combine(int n, int k) {
        3           List<List<Integer>> res = new ArrayList();
        4           Deque<Integer> path = new ArrayDeque();
```
5 //从1~n,数组下标0不可用，长度为n+1

```java
6           boolean[] used = new boolean[n+1];
7           backtrack(n,k,1,path,used,res);
8           return res;
9       }
```
10
11 public void backtrack(int n,int k,int start,Deque<Integer> path,boolean[]

```java
used,List<List<Integer>> res) {
```
12 //terminal 这里并不是递归的深度达到多少而终止，而是走过的路径中包含了k个数就需要记录一下结果

```java
13            if (path.size() == k) {
    14                res.add(new ArrayList(path));
    15                return;
    16            }
    17            for (int i=start;i<=n;i++) {
        18                if (used[i]) {
            19                    continue;
            20                }
```
21

```java
22                path.addLast(i);
23                used[i] = true;
```
24

```java
25                backtrack(n,k,i+1,path,used,res);
```
26

```java
27                path.pollLast();
28                used[i] = false;
```
29

```java
30            }
31        }
32    }
```

## 第 6 页

17. 电话号码的字母组合

亚马逊，字节半年内面试题，17. 电话号码的字母组合

思考要点：

1：给出的字符串digits有多少位，结果集中每种组合的长度就是多少。

2：有点类似括号生成，只不过不再是生成左右括号，而是生成每个字符对应的字母

每层生成的结果，及下探到下一层的过程如下图（递归的状态树）：

```java
1   class Solution {
    2       public List<String> letterCombinations(String digits) {
        3            List<String> res = new ArrayList();
```
4 //特殊情况处理

```java
5           if (digits==null || digits.length() ==0) {
    6               return res;
    7           }
    8           Map<Character,String> map = new HashMap();
    9           map.put('2',"abc");
    10           map.put('3',"def");
    11           map.put('4',"ghi");
    12           map.put('5',"jkl");
    13           map.put('6',"mno");
    14           map.put('7',"pqrs");
    15           map.put('8',"tuv");
    16           map.put('9',"wxyz");
```

## 第 7 页

```java
17           StringBuilder sb = new StringBuilder();
```
18

```java
19           backtrack(digits,0,sb,map,res);
20           return res;
21       }
```
22
23 //此处的StringBuilder sb就是我们走过的路径,
24 public void backtrack(String str,int index,StringBuilder sb,Map<Character,String>

```java
map,List<String> res) {
```
25
26 //terminal
27 if (index == str.length() ) { //相当于扫描题目所给的digits,每遇见一位字符就去生成对应的字母

```java
28               res.add(sb.toString());
29               return;
30           }
```
31 //找出待选择列表(这里和之前不一样的是，每层选择列表不一样)

```java
32           char c = str.charAt(index);
33           String st = map.get(c);
```
34
35 //这里不需要用uesd来剪枝的原因是每次选择列表不一样，不存在所谓的重复（之前是因为每次下探到下一层是还是在原选择列表做选择）

```java
36           for (int i=0;i<st.length();i++) {
    37               sb.append(st.charAt(i));
```
38 //drill down

```java
39               backtrack(str,index+1,sb,map,res);
40               sb.deleteCharAt(sb.length()-1);
41           }
42       }
43   }

```
51. N 皇后

字节，腾讯，半年内考过，51. N 皇后

经典的回溯问题，N皇后是8皇后的扩展，8皇后经常在教科书中被那出来讲解回溯算法！

参考官方题解的解法一：基于集合的回溯：https://leetcode-cn.com/problems/n-
queens/solution/nhuang-hou-by-leetcode-solution/

回溯过程参看题解：https://leetcode-cn.com/problems/n-queens/solution/gen-ju-di-46-ti-quan-pai-
lie-de-hui-su-suan-fa-si-/中的动图效果

```java
1   class Solution {
    2       public List<List<String>> solveNQueens(int n) {
        3           List<List<String>> res = new ArrayList();

```
4 //特殊判断 n=1 或 n>=4才有解

## 第 8 页

```java
5           if ((n<1) || (n>1 && n<4 )) {
    6               return res;
    7           }
```
8 //存储n皇后的放置结果,数组下标i代表放在哪行,queens[i]代表放在哪列

```java
9           int[] queens = new int[n];
```
10 Arrays.fill(queens,-1);//代表初始状态
11 //创建三个Set集合，在回溯过程中用于判断在列，撇，捺上是否已经有皇后存在了

```java
12           Set<Integer> lie = new HashSet();
13           Set<Integer> pie = new HashSet();
14           Set<Integer> na =  new HashSet();
```
15

```java
16           backtrack(n,0,queens,lie,pie,na,res);
17           return res;
18       }
```
19
20 public void backtrack(int n,int row,int[] queens,Set<Integer> lie,Set<Integer>

```java
pie,Set<Integer> na,List<List<String>> res) {
```
21
22 //terminal

```java
23           if (row == n) {
```
24 //根据queens生成返回结果

```java
25               List<String> result = genResult(queens,n);
26               res.add(result);
27               return;
28           }
```
29
30 //相当于是在行，列，撇，捺四个层面去回溯

```java
31           for (int i=0;i<n;i++) {
```
32 //剪枝1：如果该列上有皇后了则跳过

```java
33               if (lie.contains(i)) {
    34                   continue;
    35               }
```
36 //剪枝2：判断撇上是否已经有皇后了:在同一撇上的皇后，行列坐标之和是相等的

```java
37               int rowAddColumn = row + i;
38               if (pie.contains(rowAddColumn)) {
    39                   continue;
    40               }
```
41 //剪枝3：判断捺上是否已经有皇后了：在同一捺上的皇后，行列坐标之差是相等的

```java
42               int rowSubColumn = row - i;
43               if (na.contains(rowSubColumn)) {
    44                   continue;
    45               }
```
46
47 //让皇后落在该行该列上

```java
48               queens[row] = i;
```
49 lie.add(i);//该列已经有皇后了
50 pie.add(rowAddColumn);//该撇上已经有皇后了
51 na.add(rowSubColumn);//该捺上已经有皇后了
52
53 //drill down下探到下一行（下一层）

```java
54               backtrack(n,row+1,queens,lie,pie,na,res);
```
55

56 //撤销选择

## 第 9 页

```java
57               queens[row] = -1;
58               lie.remove(i);
59               pie.remove(rowAddColumn);
60               na.remove(rowSubColumn);
61           }
```
62

```java
63       }
```
64

```java
65       public List<String> genResult(int[] queens,int n) {
    66           List<String>  tempResult = new ArrayList();
```
67

```java
68           for (int i=0;i<queens.length;i++) {
    69               char[] row = new char[n];
    70               Arrays.fill(row,'.');
    71               row[queens[i]] = 'Q';
    72               tempResult.add(new String(row));
    73           }
    74           return tempResult;
    75       }
```
76

```java
77   }

```
169. 多数元素

字节，阿里，美团点评最近面试题，169. 多数元素

我们使用经典的分治算法递归求解，直到所有的子问题都是长度为 1 的数组。长度为 1 的子数组中唯一的数显然是众数，直接返回即可。

如果分治后某区间的长度大于 1，我们必须将左右子区间的值合并。如果它们的众数相同，那么显然这一段区间的众数是它们相同的值。否则，我们需要比较两个众数在整个区间内出现的次数来决定该区间的众数。

```java
1   class Solution {
    2       public int majorityElement(int[] nums) {
```
3 // 分治~合并

```java
4           return divideConquer(nums,0,nums.length-1);
5       }
```
6

```java
7       public int divideConquer(int[] nums,int start,int end) {
```
8 // terminal

```java
9           if (start == end) {

    10               return nums[start];
```

## 第 10 页

```java
11            }
```
12
13 //divide

```java
14            int mid = (end-start)/2 + start;
15            int leftMajor = divideConquer(nums,start,mid);
16            int rightMajor = divideConquer(nums,mid+1,end);
```
17
18 //conquer
19 //子区间的众数一样则直接返回

```java
20            if (leftMajor == rightMajor) {
    21                return leftMajor;
    22            }
```
23 //leftMajor在[start,end]中出现的次数为

```java
24            int leftCount = count(nums,leftMajor,start,end);
```
25 //rightMajor在[start,end]中出现的次数为

```java
26            int rightCout = count(nums,rightMajor,start,end);
```
27

```java
28            return leftCount > rightCout ? leftMajor : rightMajor;
29        }
30        public int count(int[] nums,int major,int start,int end) {
```
31 // 在[start,end]区间内统计众数major的出现次数

```java
32            int count = 0;
33            for (int i=start;i<=end;i++) {
    34                if (nums[i] == major) {
        35                    count++;
        36                }
        37            }
        38            return count;
        39        }
        40    }

```
总结

回溯算法经常用于去解决：查找所有可能的解，排列组合，这类问题
