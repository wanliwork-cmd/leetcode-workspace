# 突破栈和队列面试题

## 第 1 页

突破栈和队列面试题

面试题列表

1：腾讯，字节跳动最近面试题，232. 用栈实现队列

2：字节跳动，facebook最近面试题，225. 用队列实现栈

3：华为，网易，腾讯最近面试题，84. 柱状图中最大的矩形

4：字节跳动，华为，腾讯最近面试题，42. 接雨水

5：字节跳动，谷歌，亚马逊最近面试题，239. 滑动窗口最大值

1、232. 用栈实现队列-C

辅助栈

```java
1   class MyQueue {
```
2
3 //维护两个栈
4 Deque<Integer> left;//左栈，负责元素入栈
5 Deque<Integer> right;//右栈，负责元素出栈
6 /** Initialize your data structure here. */

```java
7       public MyQueue() {
    8           left = new ArrayDeque();
    9           right = new ArrayDeque();
    10       }
```
11
12 /** Push element x to the back of queue. */

```java
13       public void push(int x) {
    14           left.push(x);
    15       }
```
16

```java
17       private void reverse(){
```
18 //当右栈为空时,再将左栈内元素全部倒过来，否则会破坏出栈顺序

```java
19           if (right.isEmpty()) {
    20               while ( ! left.isEmpty()) {
        21                   right.push(left.pop());
        22               }
        23           }
        24       }
```
25
26 /** Removes the element from in front of queue and returns that element. */

```java
27       public int pop() {
    28           reverse();
    29           return right.pop();

    30       }
```

## 第 2 页

31
32 /** Get the front element. */

```java
33       public int peek() {
    34           reverse();
    35           return right.peek();
    36       }
```
37
38 /** Returns whether the queue is empty. */

```java
39       public boolean empty() {
    40           return left.isEmpty() && right.isEmpty();
    41       }
    42   }

```
2、225. 用队列实现栈-C

用栈模拟队列需要两个栈，用队列模拟栈只需一个即可，甚至Deque接口下的实现本身就可以当栈来使用，比如ArrayDeque， LinkedList等，因为该接口中定义了栈操作的相关方法

但题设条件你只能使用队列的基本操作-- 也就是 push to back, peek/pop from front, size, 和 is empty 这些操作是合法的。，我们不能使用Deque接口中的相关方法，只能使用Queue接口中的基本操作

```java
1   class MyStack {
```
2

```java
3       Queue<Integer> queue;
```
4
5 /** Initialize your data structure here. */

```java
6       public MyStack() {
    7           queue = new LinkedList();
    8       }
```
9
10 /** Push element x onto stack. */

```java
11       public void push(int x) {
    12           queue.add(x);
    13           for (int i = 1;i<queue.size();i++) {
        14               queue.add(queue.poll());
        15           }
        16       }
```
17
18 /** Removes the element on top of the stack and returns that element. */

```java
19       public int pop() {
    20           return queue.poll();
    21       }
```
22
23 /** Get the top element. */

```java
24       public int top() {
    25           return queue.peek();
    26       }

```
27

## 第 3 页

28 /** Returns whether the stack is empty. */

```java
29        public boolean empty() {
    30            return queue.isEmpty();
    31        }
    32    }
```
33

3、84. 柱状图中最大的矩形-C

1、朴素解法，暴力破解

```java
1   class Solution {
    2       public int largestRectangleArea(int[] heights) {
```
3 //暴力破解

```java
4           int max = 0;
5           for (int i = 0 ;i<heights.length;i++) {
```
6 //用两个指针分别向左右扩散,寻找以当前heights[i]为高度的最大矩形的左右两个下标，

```java
7               int left = i;
8               int right = i;
9               while (left > 0 ) {
```
10 left--;//向左边扩散

```java
11                    if (heights[left] < heights[i]) {
```
12 left++;//最左边的下标

```java
13                        break;
14                    }
```
15

```java
16                }
17                while (right < heights.length-1 ) {
```
18 right++;//向右边扩散

```java
19                    if ( heights[right] < heights[i]) {
    20                        right--;
    21                        break;
    22                    }
```
23

```java
24                }
25                int tempArea = (right-left+1) * heights[i];
26                max = max > tempArea ? max :tempArea;
27            }
```
28

```java
29            return max;
30        }
31    }

```
时间复杂度：O(N^2)，这里 N 是输入数组的长度。
空间复杂度：O(1)。

如何优化？

## 第 4 页

目前找以每个元素为高度的最大矩形的左右两个下标的方式是从当前位置向两边扩散，这样找的时间复杂度是O(N)的，那能否优化这个查找操作？能否控制在O(1)下

我们可以借助单调栈来完成！

单调栈

单调栈的定义：单调栈即满足单调性的栈结构。

单调栈分为单调递增栈和单调递减栈，顾名思义，就是栈内元素是升序还是降序排列的，也涉及到出栈的逻辑。

如下图，分别插入6,10,3,7,4,12的时候，单调递增栈和单调递减栈的情况分别是样子的：

单调栈如何实现，下面来实现一个单调递减栈com.itheima.stack.MonotonicityStack

1 /**
2 * 给定一个数组,返回同等代表源数组中每个元素左边第一个小于自己的数的数组,构造单调递减栈,如果左边没有小于自己的返回-1
3 * @param src
4 * @return
5 */

```java
6   private int[] getLeftMinNum(int[] src) {
    7       int[] result = new int[src.length];
    8       // Deque<Integer> monotoneStack = new LinkedList<>();
    9       Deque<Integer> monotoneStack = new ArrayDeque<>();
    10       for (int i=0;i<src.length;i++) {

        11           while (!monotoneStack.isEmpty() &&  src[i] <= monotoneStack.peek() ) {
```

## 第 5 页

```java
12               monotoneStack.pop();
13           }
14           if (!monotoneStack.isEmpty()) {
    15               result[i] = monotoneStack.peek();
    16           }else {
        17               result[i] = -1;
        18           }
        19           monotoneStack.push(src[i]);
        20       }
        21       return result;
        22   }
```
23
24

```java
25   public static void main(String[] args) {
    26       int[] param = new int[]{6,10,3,7,4,4,12,5};
    27       System.out.println(Arrays.toString(new MonotonicityStack().getLeftMinNum(param)));
    28   }

```
时间复杂度：O(N)

空间复杂度：O(N)

有单调栈，自然也有单调队列，相关概念作为课后作业自行学习！

哨兵

单调栈的优化，以上代码在编写的时候需要考虑如下情况

1、遇到入栈元素比栈顶元素小，则先需要栈顶元素出栈，一直到入栈元素大于栈顶元素，弹出栈顶元素的时候要考虑栈为空的情况，因此while循环的条件中有一个 !monotoneStack.isEmpty()，

2、以及最后当入栈元素大于了栈顶元素之后还需要判断栈不为空，则获取栈顶元素作为入栈元素左边第一个小于它的元素，否则取-1；我们需要判断栈是否为空！

解决方案：哨兵，事先在栈中存一个 -1，这样在循环入栈的整个过程中就不需要做栈空的判断了，改进后的代码如下

1 /**
2 * 单调栈+哨兵
3 * 哨兵：代表了一种边界情况，所以一般可以用哨兵来减少一些边界判断
4 * @param src
5 * @return
6 */

```java
7   private int[] getLeftMinNumOptimize(int[] src) {
    8       int[] result = new int[src.length];
    9       Deque<Integer> monotoneStack = new ArrayDeque<>();
```
10 //提前加入哨兵

```java
11       monotoneStack.push(-1);
```
12

```java
13       for (int i=0;i<src.length;i++) {
```

## 第 6 页

```java
14           while (src[i] <= monotoneStack.peek() ) {
    15               monotoneStack.pop();
    16           }
    17           result[i] = monotoneStack.peek();
    18           monotoneStack.push(src[i]);
    19       }
    20       return result;
    21   }
```
22
23

```java
24   public static void main(String[] args) {
    25       int[] param = new int[]{6,10,3,7,4,4,12,5};
```
26 System.out.println(Arrays.toString(new

```java
MonotonicityStack().getLeftMinNumOptimize(param)));
27   }

```
哨兵的作用就是：遇到哨兵就相当于栈空了！代表了一种边界情况，所以一般可以用哨兵来减少一些边界判断

单调栈+哨兵解决84题

再来看84. 柱状图中最大的矩形这一题，

```java
1   class Solution {
    2       public int largestRectangleArea(int[] heights) {
```
3 //特殊判断

```java
4           if (heights == null) {
    5               return 0;
    6           }
```
7 //构造一个新数组，安排好哨兵

```java
8           int[] h = new int[heights.length+2];
9           h[0] = -1;
10           h[h.length-1] = -1;
11           System.arraycopy(heights, 0, h, 1, heights.length);
12           Deque <Integer> stack = new ArrayDeque();
```
13 //遍历前将左侧哨兵入栈

```java
14           stack.push(0);
```
15 //定义最大面积

```java
16           int max = 0;
17           for (int i=1;i<h.length;i++) {
    18               while (h[i] < h[stack.peek()]) {
```
19 //栈顶元素对应的高度

```java
20                   int height = h[stack.pop()];
21                   max = Math.max(max, (i-1 - stack.peek())*height);
22               }
23               stack.push(i);
24           }
25           return max;
26       }
27   }
```

## 第 7 页

以下列出了单调栈的问题，供大家参考。

序号题目题解

1 42. 接雨水（困难）暴力解法、优化、双指针、单调栈

2 739. 每日温度（中等）暴力解法 + 单调栈

3 496. 下一个更大元素 I（简单）暴力解法、单调栈

4 316. 去除重复字母（困难）栈 + 哨兵技巧

5 901. 股票价格跨度（中等）单调栈

6 402. 移掉K位数字

7 581. 最短无序连续子数组

4、42. 接雨水-P

和84题有着异曲同工之妙，也可以用单调栈来解决，只不过该题需要用到单调递增栈

```java
1   class Solution {
    2       public int trap(int[] height) {
```
3 //特殊判断

```java
4           if (height == null) {
    5               return 0;
    6           }
```
7 //构造单调栈,此处两边需要哨兵,原因是因为两侧肯定是装不了水的,如果加入了哨兵要么就让两侧能装水了,要么就是导致中间的计算结果不正确

```java
8           Deque<Integer> stack = new ArrayDeque();
```
9 //定义最大值

```java
10           int max = 0;
```
11 //数组元素依次入栈

```java
12           for (int i=0;i<height.length;i++) {
    13               while (!stack.isEmpty() && height[i] >= height[stack.peek()]) {
        14                   int p = stack.pop();
```
15

```java
16                   if (!stack.isEmpty()) {
```
17 max += (Math.min(height[stack.peek()], height[i]) - height[p]) * (i-

```java
stack.peek()-1);
18                   }
```
19

```java
20               }
21               stack.push(i);
22           }
23           return max;
24       }
25   }
```

## 第 8 页

5、239. 滑动窗口最大值-P

1、暴力解法，未AC

```java
1   class Solution {
```
2 //暴力解法

```java
3       public int[] maxSlidingWindow(int[] nums, int k) {
```
4 //特殊判断

```java
5           if (nums == null) {
    6               return new int[]{};
    7           }
```
8 //定义结果数组

```java
9           List<Integer> list = new ArrayList();
10           for (int i=k-1;i<nums.length;i++) {
    11               int max = nums[i];
```
12 //k窗口内比较大小

```java
13               for (int j=i;j> i-k;j--) {
    14                   max = Math.max(max, nums[j]);
    15               }
    16               list.add(max);
    17           }
    18           int[] result = list.stream().mapToInt(Integer::valueOf).toArray();
    19           return result;
    20       }
    21   }

```
提交超时

2、单调队列

同单调栈，单调队列也分单调递增队列（队头到队尾递增，队列头是最小元素），单调递减队列（队头到队尾递减，队列头是最大元素），一般用来解决滑动窗口内的最值问题

```java
1   class Solution {
```
2 //利用单调递减队列,队列中存储元素下标

```java
3       //private Deque<Integer> queue = new LinkedList();
```
4
5 //利用单调队列解决滑动窗口的最值问题

```java
6       public int[] maxSlidingWindow(int[] nums, int k) {
```
7 //特殊判断

```java
8           if (nums == null || nums.length < k) {
    9               return new int[]{};
    10           }
```
11 //存储结果集

```java
12           int[] result = new int[nums.length-k+1];
13           int r = 0;
```
14

15 //利用单调递减队列,队列中存储元素下标

## 第 9 页

```java
16           Deque<Integer> queue = new LinkedList();
```
17

```java
18           for (int i=0;i<nums.length;i++) {
```
19 //构造单调递减队列,队列中存储的是数组元素的下标

```java
20               while (!queue.isEmpty() && nums[i] > nums[queue.peekLast()]) {
    21                   queue.pollLast();
    22               }
    23               queue.offerLast(i);
    24               if (i >= k-1) {
```
25 //保证后面获取的最大值是在滑动窗口内,只需要用当前下标-队列头(最大值)如果超过窗口大小则将队列头出队列

```java
26                   if (i - queue.peekFirst() >= k) {
    27                       queue.pollFirst();
    28                   }
    29                   result[r++] = nums[queue.peekFirst()];
```
30

```java
31               }
32           }
33           return result;
34       }
35   }
```
